import React from 'react';
import Layout from '../../components/layout/Layout';

const Profile = () => {
    return (
        <Layout>
           profile
        </Layout>
    );
};

export default Profile;